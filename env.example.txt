# Copy to .env and fill in the models you want to enable.
# The model picker only shows models whose credentials are present, so you can
# run with just one configured.

# --- Azure OpenAI (in-boundary) -------------------------------------------
# Uses the v1 API. Endpoint can be the base or include /openai/v1 — both work.
# No api-version needed. Keep values unquoted with no inline comments.
AZURE_OPENAI_ENDPOINT=https://YOUR-RESOURCE.openai.azure.com/openai/v1
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=gpt-5.4-mini

# --- Azure AI Vision (optional, in-boundary OCR for on-image highlighting) -
# Enables the per-field "locate on image" overlay. If unset, the app works
# exactly the same minus that overlay. First-party Azure service.
AZURE_VISION_ENDPOINT=
AZURE_VISION_KEY=

# --- Gemini (external benchmark) ------------------------------------------
# A Google AI Studio key is enough — no GCP project required.
# Note: external endpoint — blocked by the production firewall; benchmark use only.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3-flash